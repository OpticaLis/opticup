# Decisions Log — M4

> Per-module detail. Index summary in `../DECISIONS_LOG.md`.

---

## 2026-05-24 — M4 Scale Readiness: 10K-lead messaging infrastructure plan

**Situation:** Daniel plans ~10,000 leads within coming months. Today's 1,137-lead × 2-channel Wave 1 broadcast caused live ERP screen timeouts (Seq Scan 2,156ms on crm_message_queue with 20,637 rows). A live-hotfix index dropped it to 50ms, but the same class of failure recurs at 10K scale across multiple surfaces. Daniel wants the system ready before the load arrives, not firefighting under pressure.

**Measured reality (live 2026-05-24):**
- crm_message_queue: 20,637 rows / 23 MB (11,226 cancelled + 9,328 sent + 53 failed + 16 processing + 14 rejected)
- crm_message_log: 11,238 rows / 37 MB
- Dispatch throughput: ~15 msgs/tick × 1-min cron = ~15-55 msg/min (varies by SMS/email mix)
- Prizma active leads: 1,298
- Existing cleanup: daily cron deletes `sent` rows > 90 days — but `cancelled`/`failed`/`rejected` rows accumulate FOREVER (54% of table = cancelled rows, never cleaned)
- Live-hotfix index `idx_crm_message_queue_tenant_created` applied via MCP, NOT in git migrations

**My recommendation (SPEC decomposition — 5 SPECs, priority-ordered):**

See Master Plan section below. Key sequencing: queue lifecycle + throughput scaling are pre-10K-blocking (must land before the first 10K campaign). Screen optimization is also blocking but lower-effort. Email vendor + list hygiene can wait until volume justifies them.

**Question surfaced to Daniel:** "10K within coming months" — when is the first campaign likely to hit 5K+ leads? That date is the deadline for SPECs 1-3. SPECs 4-5 can follow.

**Daniel's response:** (pending — this plan surfaces the question)

**Lesson:** Infrastructure scaling should be planned when the load is 10-20% of the breaking point, not when it's already breaking. The 1,137-lead incident at ~6% of the target volume is exactly the right time to invest.


## 2026-05-24 — WhatsApp as third M4 channel: assessment + sequencing (SUPERSEDED by Dialog360 decision below)

**Situation:** Daniel says "I must send WhatsApp to customers" — urgent, wants it before/around the first 5K+ campaign (~1 month). M4 today has email + SMS only.

**Initial assessment (superseded):** Recommended Green API via Make. **Wrong** — contradicted the sealed M12 Communications Brief which already locked Dialog360 as the WhatsApp BSP with direct EF→360dialog path. Corrected below.

1. **send-message EF** — hardcoded `channel !== 'sms' && channel !== 'email'` guard at line 119. Change to accept `'whatsapp'`. Add recipient validation (phone required, like SMS). Add WhatsApp allowlist for test-mode (same pattern as SMS/email allowlists).
2. **Make scenario** — needs a WhatsApp route (Green API sendMessage). This is a Make-side config change, not ERP code. Daniel/ops can configure it.
3. **crm_message_templates** — allow `channel='whatsapp'`. Create Hebrew WhatsApp templates (body format: plain text, similar to SMS but can include rich formatting).
4. **dispatch-queue EF** — add WhatsApp throttling (configurable, alongside SMS/email throttles). Green API rate limit is ~1 msg/sec.
5. **Consent** — WhatsApp Business Policy requires user opt-in for marketing messages outside the 24-hour service window. Green API (unofficial) doesn't enforce this technically, but account-ban risk exists at scale. For now, practical approach: send only to leads who have a phone number + have not unsubscribed (same as SMS). Formal opt-in flow is a separate future SPEC.
6. **Short links** — `W` prefix already reserved in the channel convention.
7. **No Green API credentials in our DB** — `ui_config` has `whatsapp_phone_e164` (the number) but no `green_api_instance_id` or `green_api_token`. The Make scenario handles Green API auth directly — our EF never calls Green API; it calls Make.

**My recommendation — SEPARATE TRACK, not bundled with scale SPECs:**

The scale SPECs (queue-lifecycle, throughput, screen-audit) are infrastructure that benefits ALL channels. WhatsApp is a new dispatch path. Different concerns, different risk profiles. Bundling would delay the pre-10K-blocking work.

However, SPEC 2 (dispatch-throughput) MUST be WhatsApp-aware in its design: the `crm_dispatch_config` table should include `whatsapp_throttle_ms` and `whatsapp_daily_cap` columns from day one, even though WhatsApp dispatch isn't implemented yet. Zero extra effort — just add the columns. When the WhatsApp SPEC lands, the config surface is already there.

**Recommended sequence:**

```
Week 1-2: SPEC 1 (queue lifecycle) + SPEC 2 (throughput — 3-channel config design)
Week 2-3: SPEC 3 (screen audit) ‖ SPEC 6 (WhatsApp channel — can run in parallel)
Week 3-4: SPEC 6 completes (templates + testing + Make scenario)
```

WhatsApp as SPEC 6 (new, added to the Master Plan):

- **Scope:** EF validation lift (accept 'whatsapp'), WhatsApp allowlist, template schema expansion, WhatsApp templates for existing automation flows, dispatch-queue WhatsApp throttle wiring, short-link W-prefix convention activation.
- **Make-side:** Daniel/ops configure the WhatsApp route in Make scenario 9104395 pointing to Green API.
- **Vendor:** Green API via Make (already familiar, fastest path). Migrate to WhatsApp Business API later if volume justifies.
- **Estimated effort:** 1-2 sessions (ERP-side is small; Make-side is ops work).

**Blocking questions for Daniel:**
1. The Make scenario (9104395) that handles SMS + email dispatch — does it already have a WhatsApp route, or does one need to be added? (Architect cannot inspect Make scenarios directly.)
2. Is the existing Green API account (used for QR/catalog) capable of outbound marketing messages, or is it limited to inbound-initiated conversations?
3. Consent model: is it acceptable for now to send WhatsApp to all leads with a phone number (same gate as SMS), or does Daniel want an explicit WhatsApp opt-in before the first send?

**Daniel's response:** HARD CORRECTION — Green API is NOT acceptable for marketing blasts. Sending to 1000+ recipients gets the number banned (WhatsApp ToS violation). Must use Dialog360 (360dialog), the official WhatsApp Business API provider. This was the original plan in M12.

**Lesson:** Before recommending a vendor path, CHECK EXISTING ARCHITECTURAL DECISIONS. The M12 Communications Brief (sealed 2026-05-09) already locked Dialog360 as the WhatsApp BSP with a direct EF→360dialog architecture. I failed to read the M12 Brief before making the Green API recommendation — a clear Pattern P25 violation (verify existing before recommending). Green API stays only for its current inbound-initiated uses (QR/catalog).


## 2026-05-24 — WhatsApp SPEC 6 re-planned: Dialog360 (official WABA) integration

**Situation:** Daniel's correction: WhatsApp marketing sends MUST go through Dialog360, NOT Green API. Green API is not built for bulk marketing — 1000+ recipients = number banned. Dialog360 is the compliant path: official WABA, pre-approved templates, legal bulk sending. The M12 Communications Brief (sealed 2026-05-09, Decision #1) already specified this choice with a direct EF→360dialog REST API path (bypasses Make entirely).

**Re-assessed architecture (verified from M12 Brief + code):**

The M12 Brief defines:
- `channel_configs` table: per-tenant, per-channel vendor config with encrypted JSONB credentials (WABA ID, API keys). Iron Rule 19 compliant.
- `module_channel_routing` table: per-module override of channel defaults.
- WhatsApp dispatch: **direct EF → 360dialog REST API** — NOT through Make. Make stays only for SMS (Global SMS) and Email.
- Specific WABA number: +972 53-434-7265, described as undergoing migration from Meta-direct to 360dialog.

**This changes SPEC 6 significantly from the prior "validation lift" assessment:**

1. **New vendor integration path** — direct HTTP from send-message EF to Dialog360 API (auth, send-template-message, check-delivery-status). This is a new dispatch path alongside the existing Make webhook path. Not a simple channel string change.

2. **WhatsApp template pre-approval** — Dialog360/Meta require all marketing templates to be submitted and approved BEFORE they can be sent. Approval cycle: typically 24-48 hours, sometimes longer for marketing category. This is a **lead-time dependency** that exists outside the code pipeline — templates must be submitted to Dialog360 early, independent of ERP code work.

3. **Template model differs from SMS/email** — WhatsApp Business API templates have structured components (header, body, footer, buttons) and are identified by template_name + language_code, not free-form body text. The CRM template model needs to accommodate this (either a WhatsApp-specific body format or a separate template registration flow that maps CRM templates to Dialog360 template names).

4. **Consent model** — WhatsApp Business Policy requires explicit user opt-in for marketing messages outside the 24-hour service window. For utility messages (order updates, delivery notifications), opt-in is implicit. For marketing templates (the primary use case here), explicit opt-in is mandatory. Need a consent flag on the lead record.

5. **Per-tenant config** — `channel_configs` table with `provider='dialog360'`, `provider_credentials` (encrypted JSONB: `{ api_key, waba_id, phone_number_id }`). Iron Rule 19.

**My recommendation (SPEC 6 decomposition — 2 sub-SPECs):**

SPEC 6 is heavier than initially assessed. Recommend decomposing into:

**SPEC 6A: M4_WHATSAPP_CHANNEL_INFRA** (ERP-side, 2-3 sessions)
- `channel_configs` table (per-tenant, per-channel, encrypted credentials — from M12 Brief)
- send-message EF: accept channel='whatsapp', add Dialog360 dispatch path (direct HTTP to 360dialog REST API alongside existing Make path for SMS/email)
- dispatch-queue EF: WhatsApp throttling config (whatsapp_throttle_ms, whatsapp_daily_cap from SPEC 2's crm_dispatch_config)
- crm_message_templates: allow channel='whatsapp', add whatsapp_template_name field (maps CRM template → Dialog360 pre-approved template name)
- WhatsApp test-mode allowlist
- Consent flag on crm_leads (marketing_whatsapp_opt_in boolean)
- Short links: W-prefix convention activation

**SPEC 6B: M4_WHATSAPP_TEMPLATES_AND_LAUNCH** (ops + verification, 1 session)
- Submit marketing templates to Dialog360 for Meta approval (body text authored, template_name chosen, category=marketing, language=he)
- Map approved template names to CRM template records (whatsapp_template_name column)
- E2E send test: demo tenant → Dialog360 sandbox → verify delivery + click tracking
- Prizma go-live: configure channel_configs with production Dialog360 credentials

**Sequencing (updated):**

```
NOW                                            First 5K campaign
 │                                                    │
 ├─ SPEC 1 (queue lifecycle)                          │
 ├─ SPEC 2 (throughput — incl. WA throttle cols)      │
 ├─ SPEC 3 (screen audit)            ║                │
 ├─ SPEC 6A (WA infra) ══════════════╝                │
 │   (parallel w/ SPEC 3)                             │
 ├─ [Dialog360 template submission — ops, starts NOW] │
 ├─ SPEC 6B (templates + launch) ─────────────────────┤
 │                                   SPEC 4 (email)   │
 │                                   SPEC 5 (hygiene) │
```

**Critical timeline risk: template approval lead time.** WhatsApp marketing templates must be submitted to Dialog360 → Meta for approval. Typical cycle: 24-48 hours, but rejections require resubmission (days). **Daniel should submit templates NOW** (the Hebrew text is ready from existing SMS/email templates — just needs reformatting for WhatsApp template structure). This is ops work independent of ERP code.

**Blocking questions for Daniel (updated):**
1. **Dialog360 account:** Is it already set up? Is the WABA number (+972 53-434-7265 from M12 Brief, or the Prizma number 972533645404) approved and active?
2. **API credentials:** Do we have a Dialog360 API key? Is there a sandbox/test environment?
3. **Template submission:** Has anyone started submitting WhatsApp templates for approval? If not, this should start immediately — it's the longest lead time item.
4. **Consent:** Is there an existing opt-in mechanism for WhatsApp marketing (e.g., checkbox on the registration form), or does one need to be built?

**Daniel's response:** (pending)

**Lessons:**
1. **Check sealed Briefs before recommending.** M12 Brief already had the Dialog360 decision locked. I failed to read it. This is a P25 violation (verify existing before recommending) at the architecture level — not just code level. Must add M12 Brief to bootstrap-read list when WhatsApp/communications topics arise.
2. **Vendor compliance ≠ vendor familiarity.** Green API is familiar (already in use) but non-compliant for the use case (bulk marketing). The familiar tool was the wrong recommendation. When the use case involves bulk/marketing/ToS-sensitive sending, default to the official API provider — never the unofficial wrapper.


## 2026-05-24 — 3-way WhatsApp path comparison: Dialog360 vs Make Cloud API vs Direct Cloud API

**Situation:** Daniel discovered Make has a native "WhatsApp Business Cloud" module connecting to Meta's official Cloud API. Three paths now on the table. Daniel's priorities (ranked): (1) WhatsApp is MANDATORY with FUTURE deep integration (two-way inbox, customer-card, automation); (2) compliant for marketing to 1000+; (3) cost secondary to compliance + future-fit; (4) ~1 month to first 5K+ campaign.

**Head-to-head assessment:**

### Path A: Dialog360 (BSP) — direct EF → 360dialog REST API

| Dimension | Assessment |
|---|---|
| Compliance | Official WABA BSP, Meta-certified. Pre-approved template sending. Legal for 1000+ marketing. |
| Cost | ~$59-119/mo/number + Meta per-conversation fees (~$0.04-0.08 marketing, ~$0.01-0.02 utility, Israel). |
| Two-way inbox | **Decisive advantage:** Coexistence Mode + `smb_message_echoes` webhook. Staff WhatsApp Business app stays live on their phone. Every staff-initiated reply auto-mirrors into the system inbox — zero blind spots. This is the M12 Brief's day-1 requirement. |
| Template approval | Via Dialog360 dashboard (submits to Meta on your behalf). Streamlined. |
| EF integration | Direct HTTP to 360dialog REST API. Clean channel abstraction. No Make dependency. |
| Vendor lock-in | BSP-level lock-in. Migration to another BSP or direct Meta is possible but requires WABA number transfer (~1-2 weeks). |
| Staff phone coexistence | YES — the defining feature. Staff keeps using WhatsApp Business app daily; API runs in parallel. |

### Path B: Meta Cloud API via Make's "WhatsApp Business Cloud" module

| Dimension | Assessment |
|---|---|
| Compliance | Official Meta API (through Make). Compliant for marketing. |
| Cost | No BSP fee ($59-119 saved). Meta per-conversation fees same as A/C. Make subscription (already exists). |
| Two-way inbox | **Fatal gap:** Make's module is primarily outbound. Inbound webhook handling is limited — Make scenarios can receive webhooks but are NOT a real-time inbox system. Building the M12 two-way inbox through Make scenarios would be fragile, slow, and would need to be replaced anyway. |
| Template approval | Via Meta Business Manager directly (more manual than Dialog360 dashboard). |
| EF integration | EF → Make webhook → Make module → Meta API. Adds latency + Make as a critical dependency for a core channel. If Make has downtime, WhatsApp stops. |
| Vendor lock-in | Make becomes critical infrastructure. Migration to direct API = rewrite the dispatch path (same work as Path A/C from scratch). |
| Staff phone coexistence | Uncertain — depends on Make's module implementation. No smb_message_echoes (BSP feature). Staff phone messages invisible to system. |

### Path C: Meta Cloud API DIRECT from EF (no Make, no BSP)

| Dimension | Assessment |
|---|---|
| Compliance | Official Meta API. Compliant for marketing. |
| Cost | Cheapest: Meta per-conversation fees only. No BSP fee, no Make dependency. |
| Two-way inbox | Possible via Meta webhooks (register a webhook URL, receive inbound messages). But NO smb_message_echoes — if staff sends a message from the WhatsApp Business app on their phone, the system doesn't see it. Inbox has blind spots for staff-initiated replies. |
| Template approval | Via Meta Business Manager directly. |
| EF integration | Direct HTTP to Meta's graph.facebook.com/v18.0. Clean. More complex webhook management (Meta sends raw webhooks, we handle routing/retry/verification). |
| Vendor lock-in | Lowest — direct Meta relationship, no intermediary. |
| Staff phone coexistence | Partial since Meta's 2024 "phone number sharing" update. But without smb_message_echoes, staff replies from the Business app are invisible to the API system. |

**The decisive factor: staff phone coexistence + complete inbox.**

Prizma's staff uses WhatsApp Business app on their phone DAILY for customer communication. The M12 Brief explicitly states: *"WhatsApp is the day-1 critical-path channel — without it, the LIVE cutover regresses Prizma's daily operations."* The `smb_message_echoes` webhook (a BSP feature, not available in direct Meta API or Make) automatically mirrors every staff reply into the system inbox. Without it:
- Path B (Make): staff replies invisible. Dead end for M12 inbox. Temporary path that creates migration debt.
- Path C (Direct): staff replies invisible. Would need a BSP later for the inbox anyway. Saves $59-119/mo but at the cost of blind spots + future migration.
- Path A (Dialog360): staff replies visible via smb_message_echoes. Complete inbox from day 1. The $59-119/mo is negligible vs. the operational value of zero blind spots.

**My recommendation (ranked):**

**1. Path A: Dialog360 — CONFIRMED as the right choice.** The M12 Brief's decision stands and is strengthened by the comparison. Coexistence Mode + smb_message_echoes is a capability that neither Make nor direct Meta API can replicate. Given Daniel's priority #1 (future deep integration, two-way inbox), this is the only path that doesn't create migration debt. The $59-119/mo is the cost of a complete, production-grade WhatsApp channel with zero staff disruption.

**2. Path C: Direct Cloud API — viable ONLY if the two-way inbox is deprioritized** (not Daniel's stated priority). Saves the BSP fee but creates an inbox blind spot that would need a BSP to close later.

**3. Path B: Make Cloud API — NOT recommended.** Adds Make as a critical dependency for WhatsApp, offers no path to the two-way inbox, and would need to be replaced when M12 inbox is built. A dead end.

**Daniel's response:** (pending)

**Concrete first step this week:** Sign up for a Dialog360 account at 360dialog.com. Begin the WABA number onboarding for the Prizma WhatsApp number. Submit the first Hebrew marketing template for Meta approval. This unblocks everything — the ERP code (SPEC 6A) can proceed in parallel.

**Lesson:** When comparing vendor paths, the dimension that matters most is the one aligned with the HIGHEST-PRIORITY future requirement. Here, the two-way inbox (priority #1) makes smb_message_echoes the decisive feature — and that's BSP-exclusive. Cost analysis alone would have picked Path C; feature-weighted analysis picks Path A.
