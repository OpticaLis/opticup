# Access Lab Audit — `optic_dt_all.accdb`

**Audit date:** 2026-05-06
**File:** `C:\Users\User\opticup\tests\optic_dt_all.accdb` (10.7 MB)
**Mode:** Read-only, light scope (structure + logic only — לא נדרש למיגרציה)
**VBProject signature:** `optic_dt` (זהה ל-FE — ראה §4)

---

## 1. סיכום (5 שורות)

הקובץ הוא **back-end נתונים נקי**: 12 טבלאות משתמש, 0 טפסים, 0 דוחות, 0 queries אמיתיות, 0 logic מקומית. הוא מאוחסן 3 קטלוגים מרכזיים (מסגרות, עדשות-מגע, ספקים), 2 מערכות תפעוליות (ארגזי החזרות + זיכויים), וטבלת order_list שכפולה של ה-FE. כל ה-VBA הקיים בקובץ הוא **קוד יתום** שהועתק מה-FE (שם הפרויקט הוא `optic_dt` ולא `optic_dt_all`) — הקוד מתייחס לטבלאות שלא קיימות כאן ולכן לא רץ. אין אינטגרציות חיצוניות פעילות מתוך הקובץ הזה. **המבנה לא חושף שום היגיון עסקי שלא כבר זוהה ב-2 ה-audit הקודמים** — הקובץ הוא רק שכבת אחסון קטלוגית-תפעולית שמתבצרת ב-Access נפרד מסיבות גודל.

---

## 2. טבלאות, תפקיד, ספירות

### קטלוגים ראשיים
| טבלה | רשומות | תפקיד |
|---|---|---|
| `tb_catalog` | **8,733** | קטלוג מסגרות (חברה, דגם, גודל, צבע, ברקוד, ספק, מחירים) |
| `tb_ad_catalog` | **2,904** | קטלוג עדשות-מגע (חברה, סוג, חומר, קוטר, sph/cyl, מחירים, מלאי כולל/נוכחי) |
| `SAPAKIM` | **37** | ספקי מעבדה (קוד, שם, כתובת, טלפון, אימייל, ניכוי, מע״מ, ש"ח/דולר) |
| `TT  TELEFONE  SAPAKIM` | 2 | טלפונים נוספים לספק (כנראה temp לא בשימוש — "TT" prefix) |

### תנועות מלאי קטלוגיות
| טבלה | רשומות | תפקיד |
|---|---|---|
| `tb_upd_amount` | **13,885** | היסטוריית עדכוני מלאי למסגרות (תאריך, כמות, מחיר עלות, סוג תנועה, חשבונית) |
| `tb_upd_amount_ad` | **6,008** | היסטוריית עדכוני מלאי לעדשות-מגע — אותו מבנה |

### תהליכים תפעוליים
| טבלה | רשומות | תפקיד |
|---|---|---|
| `tb_argaz` | **496** | "ארגז" = משלוח החזרות לספק (מספר, סיבה, ספק, ברקוד, תאריך) |
| `tb_argaz_detail` | **840** | פריטי ארגז ההחזרה (קישור order_num, לקוח, קטגוריה) |
| `tb_zikuyim` | **130** | זיכויים שהתקבלו מספק (סטטוס, קטגוריה, ספק, מס׳ חשבונית, סניף, סכום) |
| `tb_zikuyim_detail` | **411** | פריטי הזיכוי (חברה, דגם, גודל, צבע, לקוח, עלות) |

### טבלאות מטא / טכניות
| טבלה | רשומות | תפקיד |
|---|---|---|
| `order_list` | 3,100 | רשימת הזמנות מה-FE — נראה ככפילות לקריאה מהירה (לא מעודכנת מ-2025-02) |
| `resp_name` | 4 | רשימת אחראים (שם, סוג, מס׳) — lookup |
| `pass` | 1 | סיסמה ראשית — שדה אחד בלבד |

**סה"כ:** 13 טבלאות משתמש (12 פעילות + 1 temp), 224 עמודות בכל הקובץ, 37 אינדקסים.

---

## 3. קשרים (Relations) — 5 בלבד

הקובץ מצומצם בקשרים מוצהרים — רק 5 רשומים פורמלית:

```
SAPAKIM (CODE SAPAK)        ──1:M──>  TT TELEFONE SAPAKIM
tb_catalog (wnum)           ──1:M──>  tb_upd_amount (rnum)
tb_ad_catalog (wnum)        ──1:M──>  tb_upd_amount_ad (rnum)
tb_argaz (wnum)             ──1:M──>  tb_argaz_detail (wnum)
tb_zikuyim (wnum)           ──1:M──>  tb_zikuyim_detail (wnum)
```

**הערות:**
- מפתח ראשי מוגדר רק על `SAPAKIM.CODE SAPAK`. כל שאר הטבלאות משתמשות ב-`wnum` (UNIQUE NOT PK) כאשר `wnum` = מזהה פנימי, ו-`rnum` = FK חיצוני.
- אין קשר רשמי בין `SAPAKIM` ל-`tb_catalog` או `tb_ad_catalog`, למרות ששתיהן מחזיקות שדה `sapak`. הקישור הוא טקסטואלי בלבד (שם הספק כמחרוזת, לא קוד).
- אין קשר ל-`order_list` בכלל — הטבלה צפה לבדה.

---

## 4. לוגיקה שזוהתה — אפס

| אובייקט | כמות | משמעות |
|---|---|---|
| Forms | **0** | אין UI — קובץ נתונים בלבד |
| Reports | **0** | אין דוחות מקוריים |
| Queries (פעילים) | **0** | רק 2 auto-generated combo-box queries (`~sq_c*`) שמצביעות על טבלת `qa_descs` שלא קיימת בקובץ הזה (linked מה-FE) |
| Macros | 1 | `Macro1` — Macro מערכת ריק/מורש מ-Access ישן |
| VBA Modules | 15 | **כולם יתומים** — ראה הסבר ⤵︎ |

**ה-VBA הוא העתק מה-FE:**
- שם פרויקט ה-VBA: `optic_dt` (זהה למאסטר ה-FE — לא `optic_dt_all`)
- כל המודולים (`mymod`, `mymod1`, `mymod2`, `scan`, `s_lang`, `Send_SMS_MDL`, `Send_watsp`, וכו') זהים בשמותיהם למודולים שתועדו ב-`ACCESS_FRONTEND_AUDIT_REPORT.md`
- הקוד מתייחס לטבלאות שלא קיימות כאן (`add_order`, `qa_descs`, `qbasis_descs`) → לא יכול לרוץ
- מסקנה: כשהוחלט להוציא את הקטלוגים ל-BE נפרד, מי שעשה את זה ביצע split פשוט וסחב גם את עץ ה-VBA במקום לנקות. **זו פסולת קוד, לא לוגיקה אמיתית.**

---

## 5. אינטגרציות חיצוניות — אפס פעילות (זוהו רק במקור)

שני המודולים שיכולים היו להיות אינטגרציות **לא רצים מהקובץ הזה** (ראה §4), אבל מכיוון שהם זהים ל-FE — הנה תזכורת קצרה למה הם עושים שם:

| מודול | מהות | סטטוס בקובץ הזה |
|---|---|---|
| `Send_watsp.SendWMessage` | WhatsApp Business דרך **green-api.com** (POST JSON) — קורא `token` + `instance` מטבלת `add_order` ב-FE | קוד נוכח, לא רץ (אין `add_order`) |
| `Send_SMS_MDL` | SMS דרך **itnewsletter.co.il** (SOAP) — **כל הפונקציה מסומנת כהערה** | מת. רק `wpp()` חי, שמפעיל URL scheme של WhatsApp Desktop דרך IE — קוד צרכני שביר |

**לא נמצאה כל אינטגרציה ייחודית לקובץ ה-BE הזה** (אין API ספקים, אין EDI, אין import סקריפטים).

---

## 6. הצעת מיפוי למודולים ב-OpticUp

הנתונים בקובץ הזה לא נדרשים למיגרציה (לפי הנחיית Daniel), אבל ה-**מבנה** מצביע על מודולים ש-OpticUp צריך לכסות. כדאי לוודא שלפחות אלה קיימים או מתוכננים:

| נושא Access | מודול OpticUp רלוונטי | סטטוס תכנון |
|---|---|---|
| `tb_catalog` (קטלוג מסגרות) | **Module 1 — Inventory** (קיים — `frames`, `frame_models`) | ✅ מכוסה |
| `tb_ad_catalog` (קטלוג עדשות-מגע) | **Module 1 — Inventory** (אם CL מתוכננת — לבדוק MODULE_SPEC) | ⚠️ לוודא |
| `SAPAKIM` (ספקי מעבדה) | **Module ? — Suppliers / Lab Suppliers** | ⚠️ **חסר?** ראה הערה למטה |
| `tb_upd_amount*` (היסטוריית מלאי) | **Module 1 — Inventory** (`writeLog` + `inventory_movements`) | ✅ מכוסה |
| `tb_argaz*` (משלוחי החזרה לספק) | **Module ? — Returns to Supplier / RMA** | ⚠️ **לוודא** — האם זה מודול נפרד או חלק מ-Inventory? |
| `tb_zikuyim*` (זיכויים מספק) | **Module ? — Supplier Credits** | ⚠️ **חסר?** — לבדוק האם מנהל-החנות מתכוון לזה ב-Module Finance עתידי |
| `order_list` | **Module 4 — CRM / Orders** | ✅ מכוסה (לב המודל הראשי) |
| `resp_name` | **Module 2 — Platform Admin / Users** | ✅ מכוסה |

### שלוש הנקודות שכדאי לוודא לפני סגירת התכנון:

1. **קטלוג ספקי מעבדה (SAPAKIM)** — האם כבר קיימת בתכנון `lab_suppliers` (שונה מ-`vendors`/`suppliers` רגילים)? ב-Access הם נשמרים בנפרד, וכך כדאי גם ב-OpticUp כי השדות שונים (יש `nikuy`, `qmaamg`, `sosek` שלא רלוונטיים לספק רגיל).
2. **תהליך החזרות לספק (`tb_argaz`)** — "ארגז" הוא תהליך תפעולי שלם: בוחרים פריטים שיש להחזיר, יוצרים מסמך, שולחים לספק, מסמנים כשחוזר זיכוי. **זה מודול-בפני-עצמו**. לוודא שהוא ב-roadmap.
3. **זיכויים מספק (`tb_zikuyim`)** — תהליך שמתחיל ב-`tb_argaz` ומסתיים בזיכוי כספי. **תלוי ב-Module Finance**. לוודא שה-flow מתואם.

---

## 7. הערות שולית

- שדות חשודים לסניטציה לפני כל מבט נוסף: `pass.main` (סיסמה אחת בלבד — כנראה סיסמת אדמין למשרד; בעת מיגרציה — לא להעתיק).
- הערה על privacy: שמות ספקים (`SHEM SAPAK`) נצפו אבל לא צוטטו בדו"ח. מחירי קטלוג לא נדגמו (light scope).
- VBProject: לא מוגן בסיסמה (`Protection=0`), ניתן להציג קוד באופן חופשי.

---

**סוף הדו"ח.** סה"כ: 12 טבלאות פעילות, 5 קשרים, 0 לוגיקה אמיתית, 0 אינטגרציות פעילות — קובץ אחסון נטו.
