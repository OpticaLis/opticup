טען את הסקיל opticup-strategic. אתה במצב מחקר אסטרטגי — audit, לא SPEC.

# למה אנחנו עושים את זה

פריזמה רצה על Access. בנינו עד עכשיו רק "ידיים" של OpticUp (CRM, מלאי, Storefront) שאוחזות בה מבחוץ — בלי באמת להבין מה מסתתר שם בפנים.

ההחלטה האסטרטגית: לבנות את כל המודולים החסרים (לקוחות, מרשמים, הזמנות, מעבדה, דו"חות, מועדון, תורים), ואז לעשות cutover אחד גדול מ-Access ל-OpticUp.

הדו"ח שלך הוא הבסיס לתוכנית הזו. בלעדיו אנחנו בונים על אינטואיציה. אחריו יש מפה של כל מה שצריך להחליף.

# המטרה

לקרוא את הקובץ:
**`C:\Users\User\opticup\tests\optic_dt.accdb`** (25MB, Access .accdb)

ולהוציא דו"ח audit שעונה על:
- מה Access מנהלת ביום-יום
- איזו לוגיקה עסקית מסתתרת בטבלאות / queries / forms / VBA
- אילו workflows של עובדים מוטבעים
- מה הקשרים בין הישויות (לקוח → הזמנה → מסגרת → עדשות → תשלומים → מעבדה → תורים → מועדון)
- מה יש שם שלא ידענו שקיים

# חוקים שלא משתנים

**Read-only.** לא לכתוב שינוי לקובץ. אם נדרש לטפל בו — צור עותק זמני במקום אחר. הקובץ המקורי לא נוגע.

**פרטיות — קריטי.** הדו"ח חייב להיות בטוח לפרסום פנימי. אסור לחשוף:
- שמות לקוחות, טלפונים, אימיילים, כתובות, ת"ז
- ערכי מרשמים: sphere, cylinder, axis, add, PD
- פרטי הזמנה ספציפיים מקושרים ללקוח
- מספרי חשבוניות / קופה אישיים

מותר ורצוי: שמות טבלאות + עמודות + טיפוסים, מפתחות, יחסים, אינדקסים, ספירות מצרפיות, אחוזי מילוי, schema של queries, structure של VBA, דוגמאות סינתטיות / anonymized להמחשת מבנה. בספק — לא לכלול.

# חופש לחקור

זה לא checklist. אתה במצב חקר. תחקור על דעת עצמך, תתבונן, תהפוך בדברים, תשאל "למה זה ככה?". דברים שיכולים להיות חשובים בלי שביקשתי:
- טבלאות יתומות (= remnants של עבר)
- כפילות מידע בין טבלאות (= סיכון cutover)
- queries מורכבות שמכילות לוגיקה עסקית (= הקסם שלא רואים מבחוץ)
- מאקרואים / VBA שרצים בלוח זמנים (= אוטומציות שצריך לשחזר)
- שדות שמרמזים על אינטגרציות חיצוניות (קופה, ספקי עדשות, בנק)
- דפוסי תאריך שמרמזים על תהליכים (created/modified/sent/printed/paid)
- שמות שדות לא תואמים לטבלה (= refactor שלא קרה)

אם מצאת משהו — תכתוב.

# Deliverable

תיקייה: `C:\Users\User\opticup\__LAUNCH_PLAN_DRAFT__\access-audit\`
דו"ח: `ACCESS_AUDIT_REPORT.md` בעברית.

המבנה גמיש — אתה האסטרטג, תבחר מה שמעביר את התובנות הכי טוב. שני דברים בטוחים שאצטרך:
- **מפת ישויות** (mermaid עדיף) של הטבלאות הראשיות + יחסים
- **הצעת מיפוי** של כל טבלה ב-Access למודול ב-OpticUp (M5/M6/M7/M9/M11/M13/Appointments) — או "לא מתאים לאף מודול קיים"

# קצב

בלי הגבלת זמן. תעשה את זה לעומק, לא במהירות. עדיף audit שלם וטוב על מהיר וחלקי. אם בדרך עולה צורך לחקור רחב יותר — תרחיב.

# בסיום

- שמור את הדו"ח.
- `git status` כדי לראות מה נוצר.
- **בלי `git add`, בלי commit.** Daniel סוקר קודם.
- כתוב הודעה אחת קצרה: "הדו"ח מוכן ב-`__LAUNCH_PLAN_DRAFT__/access-audit/ACCESS_AUDIT_REPORT.md`. סיכום: [3 שורות מה גילית]."
