# Pending entry — D-M1-09 reframing decision (post Night Pipeline)

**Created by:** opticup-architect (Cowork) 2026-05-16 morning
**Purpose:** apply the D-M1-09 reframing decision to skill-side decisions file. Cowork cannot write to `.claude/skills/` directly. The Layer 1 sweep in the next Claude Code session applies the changes, then deletes this file.

---

## File 1 — append to `.claude/skills/opticup-architect/references/decisions/M1.md`

**Append at end of file** (after the existing `2026-05-15 — Module 1 Lens Close Ceremony` section appended by the prior pending-entry, OR — if that prior pending-entry has not yet been swept — append AFTER it):

```markdown

---

## 2026-05-16 — D-M1-09 reframed (Night Pipeline Tier-3 deferral outcome)

**Context:** The Night Pipeline `M1_LENS_PHASE_2_COMPLETION` (2026-05-15 night) attempted Part A — extract a shared goods-receipt component into Module 1.5 per the original D-M1-09 promise. Empirical analysis of all 8 lens-receipt files (632 lines) + 5 frames-receipt files (~1,400 lines) found **~0 shareable lines**. The two flows share verbal descriptions ("supplier picker", "line list", "save flow") but their data models, UX paradigms, and server-side architectures are fundamentally different:

- **Frames goods-receipt:** OCR-first invoice reconciliation flow built April-2026. Reads scanned invoices → AI extracts line items → human reviews + corrects → server-side reconciles against expected delivery.
- **Lens goods-receipt:** Structured PO-close via atomic RPC built 2026-05-15. Reads PO → server-side fetches lines → human confirms received qty per line + adds manual bonus lines → atomic K2 RPC closes the receipt.

The Brief's premise of a "generic Module 1.5 component" was at the wrong axis — there is no shareable code, only shareable UX-shape expectation.

**Decision (Daniel-approved 2026-05-16):** Option B — reframe D-M1-09 as a **UX-consistency mandate** rather than a code-extraction promise.

**New formulation of D-M1-09 (replacing the original):**

> Every goods-receipt screen across product categories (frames / lenses / contact-lenses / accessories / any future category) MUST present a visually + interactively consistent experience to the staff member, regardless of underlying technical implementation:
>
> - **Same layout grid** — supplier picker top, line list middle, totals + discrepancy chip bottom, save/cancel bar fixed
> - **Same component primitives** from the Hybrid+Navy design system — same buttons, same form inputs, same modal patterns, same discrepancy chip styling
> - **Same manual-add affordance pattern** — same banner location, same modal shape, same validation feedback
> - **Same discrepancy display** — same chip pattern, same color semantics (under / over / exact)
> - **Same save/cancel pattern** — same button placement, same confirmation modal
>
> Each product category retains its own server-side architecture (RPC signature, data model, integration with category-specific tables). The UX consistency is enforced at the **design-system level**, not the code-extraction level.
>
> This decision is tracked through the `M1_5_DESIGN_SYSTEM_*` SPEC series (Hybrid+Navy migrations) — NOT through M1-internal refactor SPECs.

**What changes operationally:**

1. **Original D-M1-09 (code-extraction) is closed as RESOLVED-VIA-REFRAMING** — the empirical Night Pipeline analysis is the closing evidence.
2. **New D-M1-09 (UX-consistency mandate) is tracked through the design-system migration SPECs** — same series that already shipped Migrations #1-#4 (Suppliers Debt / Settings+Permissions / CRM / Storefront Studio).
3. **Contact-lenses + accessories phases are NO LONGER GATED on a Module 1.5 generic receipt component** — they can proceed in parallel to M7 build. Each new product category brings its own receipt screen with its own server logic, BUT MUST honor the design-system UX consistency mandate.
4. **The new `lens-nav-strip.js` widget shipped in Part D of the Night Pipeline IS the model for cross-screen consistency** — single source of truth for the navigation experience across the 7 lens screens. Future product categories may add similar nav strips (or extend an even-more-generic widget at Module 1.5 level once 3+ categories exist).

**Pattern caught + promoted to skill (P-AR-13 NEW):**

> **P-AR-13 (HIGH) — Architecture commitments must be tested against code reality before they become Brief load-bearing.**
>
> D-M1-09 was a code-extraction commitment authored before any lens-receipt code existed. The commitment carried forward through 4 SPECs (Phase 1B Procurement violated it; Gap Closure noted the violation; the Strategic Audit elevated it; the Night Pipeline attempted to fix it). Only at the final attempt did the empirical reality surface: zero shareable code.
>
> **Rule:** when an Architecture Brief includes a commitment about future code structure ("X will be extracted into Y", "Z will be a shared component"), the next Brief that touches that area MUST validate the commitment against actual code before propagating it. The validation is a single `grep + line-count + responsibility-mapping` exercise — ~30 minutes.
>
> **ROI:** prevents 4-SPEC-deep promise propagation when the original commitment was architecturally false. M1 Lens just paid that cost in real time.
>
> **Application:** every Architect Brief that references a prior architectural decision must include a §"Decision Reality Check" sub-section validating the prior decision is still anchored in code, or explicitly noting it as a forward-promise.

**M1 decision-log entry index:** D-M1-09 (original 2026-05-14) — superseded by D-M1-09-REFRAMED (2026-05-16). Original kept in module decisions log for historical context with `STATUS: REFRAMED 2026-05-16` header.
```

---

## File 2 — append to `.claude/skills/opticup-architect/SKILL.md`

**Append after the last P-AR-XX section:**

```markdown

### P-AR-13 (HIGH) — Architecture commitments must be tested against code reality before they become Brief load-bearing.

**Promoted to skill 2026-05-16 (M1 Lens Night Pipeline D-M1-09 reframing).**

When an Architecture Brief includes a commitment about future code structure ("X will be extracted into Y", "Z will be a shared component in Module N.M", "the generic receipt component"), the NEXT Brief that touches that area MUST validate the commitment against actual code before propagating it forward.

**The trap:** Decision-time commitments about code shape are made before the code exists. They can be wrong. If left unchallenged, they cascade through 3-5 SPECs as load-bearing assumptions, until a refactor SPEC attempts to honor them and discovers they are architecturally false.

**Evidence (M1 D-M1-09):** the "generic Module 1.5 component" promise was authored 2026-05-14 before lens-receipt code existed. It propagated through 4 SPECs (Phase 1B Procurement violated it; Gap Closure noted the violation; Strategic Audit elevated it to a HIGH finding; Night Pipeline Part A attempted to fix it). The Night Pipeline's empirical analysis found 0 shareable lines between frames-receipt and lens-receipt. The original commitment was wrong; reframing was the right move.

**The rule:**

Every Architect Brief that references a prior architectural decision MUST include a §"Decision Reality Check" sub-section that does ONE of:

1. **Validate** — confirm the prior decision is still anchored in current code (grep + responsibility-mapping). Cite the code locations.
2. **Reframe** — explicitly note the prior decision needs re-examination, with a recommendation for closure-as-RESOLVED OR reframing to a different axis.
3. **Defer** — explicitly mark the prior decision as "forward-promise, not yet code-bound; next code-touching SPEC will validate."

**Application timing:**
- For SPECs that build NEW code → defer (option 3) is acceptable
- For SPECs that REFACTOR existing code per a prior commitment → validate (option 1) is mandatory
- For SPECs whose execution proves the commitment infeasible → reframe (option 2) with empirical evidence

**ROI:** prevents 4-SPEC-deep promise propagation when the original commitment was architecturally false. M1 paid that cost; future modules should not.

**Anti-pattern caught:** my own Strategic Audit (2026-05-15 evening) elevated D-M1-09 to a HIGH finding without doing this check. A 30-minute reality-check would have surfaced "0 shareable lines" before the Night Pipeline was authored — and the Brief would have started with the reframing question instead of attempting the refactor first.
```

---

**End of pending-entries instructions.** After applying, delete this file.
