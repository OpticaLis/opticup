# Supabase — read-only access (anon key)
PUBLIC_SUPABASE_URL=https://tsxrrxzmdxaenlvocyit.supabase.co
PUBLIC_SUPABASE_ANON_KEY=REPLACE_ME

# Supabase service role — server-side only, NEVER expose to client
SUPABASE_SERVICE_ROLE_KEY=REPLACE_ME

# Default tenant slug (used for dev/preview)
PUBLIC_DEFAULT_TENANT=prizma
