# FINDINGS — M3_STOREFRONT_FB_CAPI_EVENT_ID_HANDOFF

> **Executor:** opticup-executor (Sonnet 4.6)
> **Date:** 2026-05-15
> **SPEC folder:** `modules/Module 3 - Storefront/docs/specs/M3_STOREFRONT_FB_CAPI_EVENT_ID_HANDOFF/`

---

## F-1 — SPEC File References for Supersale Form Are Inaccurate

**Severity:** LOW (SPEC defect, not a code defect)
**Location:** SPEC.md section 0 Pre-Authoring Reality Check + section 8 Expected Final State
**Discovery:** Pre-flight file enumeration

**Description:**
The SPEC identifies supersale-stock/index.astro and supersale-takanon/index.astro as
the supersale form files. Both are content-only pages with no form or lead-creating
fetch() calls. The actual supersale form is code-generated by:
  src/lib/shortcodes/lead-form-validation.ts::buildScript()

This function is invoked by src/lib/shortcodes/lead-form.ts::renderLeadForm() from
CMS content blocks. The SPEC author mapped URL routes, not the code generation layer.

**Impact:** +15 minutes pre-flight investigation. No incorrect code changes made.

**Suggested next action:** Update opticup-strategic skill's storefront-form authoring
protocol to grep for the actual fetch() call to the target EF before mapping file
locations. See EXECUTION_REPORT.md P-EXEC-1.

---

## F-2 — No HE/EN/RU Supersale Page Variants in Storefront

**Severity:** INFO (documentation accuracy)
**Location:** SPEC.md section 0 Baselines + section 8 Expected Final State
**Discovery:** Pre-flight page enumeration

**Description:**
The SPEC references HE/EN/RU supersale variants multiple times. No such pages exist:
no src/pages/en/supersale* or src/pages/ru/supersale* routes. Supersale is Hebrew-only
at the route layer. The single buildScript() edit covers all language contexts.

**Suggested next action:** Update KNOWLEDGE_MAP.md Gap 5 to clarify supersale is
Hebrew-only at route layer. Low priority.

---

## F-3 — Both Approved Test Phones Pre-Existing in Demo crm_leads

**Severity:** INFO (test infrastructure state)
**Location:** Supabase demo tenant crm_leads table
**Discovery:** E2E test phase

**Description:**
Both approved test phones (+972537889878, +972503348349) have non-deleted rows in
demo crm_leads. The lead-intake EF duplicate protection returned HTTP 409 for both,
which does NOT store fb_event_id. Resolution: soft-deleted the source='manual' row
for +972537889878 (id: 152e6188) via Level 2 DML to allow fresh E2E submission.

**Suggested next action:** Module Close Ceremony cleanup SPEC should soft-delete old
E2E test leads (source LIKE '%test%' OR source='manual') to keep approved phones
available for future E2E runs. See EXECUTION_REPORT.md P-EXEC-2.

---

## F-4 — verify:full Pre-Existing Violations (Legacy Archive Files)

**Severity:** INFO (pre-existing tech debt, out of scope)
**Location:** docs/wp-*.html, docs/debug-general-output.html, src/pages/api/leads/submit.ts
**Discovery:** npm run verify:full exit 1

**Description:**
verify:full returned exit 1 with 60 violations: 22 file-size (docs/wp-*.html archives),
32 rule-23-secrets (JWT tokens in static HTML archives), 6 rule-24-views-only (legacy
/api/leads/submit.ts). All 60 pre-date this SPEC. verify:staged on modified files
returned only 1 pre-existing authorized violation (EF_LEAD_INTAKE_ANON_JWT constant).

**Suggested next action:** Future cleanup SPEC: move docs/wp-*.html to _archive/ and
audit src/pages/api/leads/submit.ts for removal. TECH_DEBT level.

---

End of FINDINGS.md. 4 findings: 0 CRITICAL, 0 HIGH, 0 MEDIUM, 1 LOW, 3 INFO.
