# Activation Prompt — M3_STUDIO_TRANSLATIONS_BRAND_FILTER

Copy the block below into Claude Code on the Windows desktop:

---

טען את skill `opticup-executor` והרץ את ה-SPEC הבא:

`modules/Module 3 - Storefront/docs/specs/M3_STUDIO_TRANSLATIONS_BRAND_FILTER/SPEC.md`

זה תיקון של ~3 שורות בקובץ `modules/storefront/studio-translations.js`. אפס שינויים ב-DB, אפס נגיעה בסטורפרונט הציבורי. הרץ QA על localhost לפני commit, וודא ש-Studio → שפות → מותגים מציג 47 מותגים (ולא 155). אחרי commit כתוב EXECUTION_REPORT.md + FINDINGS.md (אם יש) באותה תיקייה.
