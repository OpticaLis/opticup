# Site Overseer — HANDOFF

**Last updated:** 2026-05-13 (after Pixel verification — Pixel 304574492100180 confirmed correctly wired + consent-gated; **REC-SITE-020 CLOSED 2026-05-13** via M3_QUICK_REGISTER_MARKETING_PRETICK_REMOVAL — pre-tick removed from `/quick-register/` marketing checkbox, storefront commit `ac6eef6`, PR pending Daniel merge; REC-SITE-021 DEFERRED for text expansion + Lead pixel wiring)
**Mode:** **Mode B** (post-discovery — SITE_MAP.md + SITE_OVERSEER_SKILL.md v0.2 baseline exists)
**Site Overseer state:** Ready to receive targeted Mode-B audits; knowledge map now loaded so structure questions resolve in <2 min via lookup vs ~20 min re-discovery

---

## What changed since last HANDOFF (2026-05-06 → 2026-05-07)

The promised Mode A discovery scan has been executed. SPEC `M3_SITE_COMPREHENSIVE_REVIEW` ran a comprehensive read-only audit of `https://prizma-optic.co.il`, produced:

1. `modules/Module 3 - Storefront/docs/specs/M3_SITE_COMPREHENSIVE_REVIEW/SITE_AUDIT_REPORT.md` — 44 findings (4 CRITICAL, 11 HIGH, 16 MEDIUM, 7 LOW, 6 INFO).
2. `roles/site-overseer/SITE_MAP.md` — first-version site map (Mode A baseline).
3. `roles/site-overseer/DECISIONS_LOG.md` — empty stub for Mode B accumulating decisions.

Site Overseer formally transitions Mode A → Mode B with this HANDOFF.

---

## Top headlines from the audit (Daniel's eyes-only triage)

### Triage tier 1 — direct customer harm right now

1. **`053-434-7265` is rendered on every homepage** as the contact phone. Same incident class as `050-717-5675`. Provenance unconfirmed; could be a defunct WP-era line. Owns 24 CMS body rows + 1 misnamed file (`public/images/lab/israel-hayom-logo.png` is actually HTML containing the number) + the deprecated `_deprecated/legal-terms.ts` artefact. (Audit findings: FIND-003 / FIND-022 / FIND-056.)
2. **`/terms/`, `/privacy/`, `/deal/` return 200 with empty body** — statutory legal pages broken in all 3 languages. Likely renderer bug for `bt='string'` CMS rows. (FIND-002.)
3. **6 Hebrew-slug pages return 500** under raw-UTF-8 fetch (`/בלוג/`, `/צרו-קשר/`, `/שאלות-ותשובות/`, `/משלוחים-והחזרות/`, `/מיופיה/`, `/משקפי-מולטיפוקל/`). Vercel apex→www redirect mis-encodes UTF-8 path bytes as Latin-1. Most browser clicks pre-encode and survive, but scrapers, paste-shares, and crawlers all 500. (FIND-001.)
4. **Lead-form confirmation emails go out branded "Optic Up Leads"** — direct violation of `feedback_customer_facing_branding`. Single fix in `src/pages/api/leads/submit.ts:148-163`. (FIND-016.)

### Triage tier 2 — fast wins

5. `tenants.business_email` is NULL → footer email link is hidden. Set it. (FIND-005.)
6. Footer link `/multifocal-guide/` returns 404. (FIND-010.)
7. `/multi-takanon/` exists in HE only — EN/RU footer links go to fallback. (FIND-013.)
8. 13 `.astro` pages contain `tenant?.name ?? 'Optic Up'` fallback. Replace with safer fallback. (FIND-017.)
9. Iron Rule 25 image-proxy violations — 6+ pages render direct `*.supabase.co/storage/...` URLs. (FIND-052.)

---

## Active task

**Awaiting Daniel's read of `SITE_AUDIT_REPORT.md` + decisions on Triage tier 1 cluster.** Site Overseer recommends Daniel produce SPECs for at least: (a) the `053-434-7265` provenance + cleanup, (b) the empty-body `/terms/` `/privacy/` `/deal/` renderer fix, (c) the lead-form FROM-name fix.

**No active hot-fix.** No emergency.

---

## Open recommendations for follow-up SPECs

| ID | Severity | Description | Estimated SPEC scope |
|---|---|---|---|
| REC-SITE-001 | (closed) | M4_HARDCODED_DEMO_PHONE_CLEANUP — closed 2026-05-07. | — |
| REC-SITE-002 | (closed) | CMS-row half closed by M3_PHONE_TEMPLATING_AND_CLEANUP (2026-05-07). File-level half closed by storefront commit `a4723b5` (2026-05-07, by Daniel) which deleted all 3 named artifacts. M3_PHONE_434_LEGACY_CLEANUP (2026-05-08) provided the documentation closure + Site Overseer audit trail. The `prizma-optice.co.il` typo: 0 occurrences in live CMS; 5 file-level occurrences are in `docs/` historical archives (preserved per discipline) + `scripts/seo/output/` cached files (auto-regenerable). Closed-as-no-action. | — |
| REC-SITE-003 | (closed) | M3_CMS_BLOCKS_RESTORE_AND_GUARDRAIL — closed 2026-05-08. 16 rows restored, 2 CHECK constraints active (cannot recur). | — |
| REC-SITE-004 | (closed) | NO-ACTION (2026-05-08, Daniel verification). All 6 Hebrew-slug pages (`/בלוג/`, `/צרו-קשר/`, `/שאלות-ותשובות/`, `/משלוחים-והחזרות/`, `/מיופיה/`, `/משקפי-מולטיפוקל/`) load fine in real browsers — verified by Daniel via direct browser test. The original audit's 5xx finding was via raw-UTF-8 curl that bypasses normal browser pre-encoding. Real customers, browsers, and search-engine crawlers all pre-encode UTF-8 paths so they hit the working `%D7...`-encoded path. No customer-facing harm; closing with no fix. | — |
| REC-SITE-005 | (closed) | NO-ACTION (2026-05-08, Daniel directive). The `submit.ts:148-163` code path is NOT wired to any active form on the live site (no contact form connected, no lead-intake form active). All ACTIVE customer-facing emails (supersale campaign, lead-intake EF) already send from `events@prizma-optic.co.il` and `service@prizma-optic.co.il` — verified. The "Optic Up Leads" string lives only in dead code. | — |
| REC-SITE-006 | (closed) | M3_TENANT_NAME_FALLBACK_SAAS — closed 2026-05-08. Replaced 28 hardcoded `?? 'Optic Up'` (audit anticipated 13; live state was 28) with `resolveTenantNameFallback(Astro.request, locale)` backed by build-time-generated static JSON map (`src/data/tenant-fallback-map.json`). Generator (`scripts/generate-tenant-fallback-map.mjs`) queries `v_public_tenant` + `v_storefront_config` per build. SaaS-clean: future tenant onboarding requires zero code changes. 11/11 unit tests PASS, Optic Up leak CLEAN. Storefront commit `a8c2acd` merged to main. | — |
| REC-SITE-007 | (closed) | M3_IMAGE_PROXY_ENFORCEMENT — closed 2026-05-09. Root cause: pre-existing `resolveStorageUrl()` had passthrough bug for full http(s) Supabase URLs; 3 DB rows (tenants.logo_url + 2 brands.logo_url) leaked direct URLs into HTML. Fix: regex-rewrite at the helper + extend proxy to support `tenant-logos/` bucket + build-time check (`scripts/check-no-direct-supabase-image.mjs`) chained to `astro build`. Live verification on production: 0 supabase URLs across 14 customer pages + 404 page (after fix-up commit `af32ad9` for unrelated `404.astro` tenant-leak bug). 6 findings logged including REC follow-up M3-COMPLIANCE-04 (bucket privacy flip). | — |
| REC-SITE-008 | (closed) | 2026-05-08, Daniel directive: chose `service@prizma-optic.co.il`. Applied via `UPDATE tenants SET business_email='service@prizma-optic.co.il' WHERE slug='prizma'`. Verified live in `v_public_tenant`. Footer email link now visible. | — |
| REC-SITE-009 | (closed) | M3_BRANCHES_INFRA_AND_ASHKELON — closed 2026-05-09. Per-branch design (NOT tenant-level address/hours per Daniel's preference for future multi-branch growth). New `tenant_branches` table + `v_storefront_branches` view (canonical RLS, Iron Rule 13/14/15/18). Storefront `/branches/` + `/branches/[slug]/` × 3 langs with Schema.org `OpticalStore` JSON-LD (10 OpeningHoursSpecification entries supporting Tuesday-no-afternoon nuance + lunch breaks; 4 gallery images via proxy; sameAs GMB). First branch live: Ashkelon. verify-branches.mjs PASS 7/7 on production. Required fix-up commit `ae60b37` to remove a pre-existing vercel.json /branches/ redirect that intercepted the new index. Future branches/tenants: 1 DB row each, no code changes. | — |
| REC-SITE-010 | (closed) | M3_COOKIE_CONSENT_OPT_IN — closed 2026-05-09. Tenant-config-driven Opt-In banner on production. All 5 trackers (GTM, GA4, FB Pixel, Hotjar, TikTok) wrapped in `consentGate()`. Pre-script stamps `window.__consent` before tracker payloads evaluate. SaaS-clean: `tenants.ui_config.cookie_consent` (v1 schema) drives banner; future tenant onboarding requires only DB seed. Live tests 6/6 PASS via Chrome DevTools MCP — Reject all → 0 tracker requests; Accept all → FB Pixel fires; UTMs unaffected; revoke from footer works. View extension `v_public_tenant.ui_config` added (Daniel-authorized Level 3 mid-flow). 6 findings logged. | — |
| REC-SITE-011 | (closed) | M3_SITEMAP_CONSOLIDATION — closed 2026-05-09. Single source of truth: `sitemap-dynamic.xml` (364 `<loc>` + 986 hreflang alternates, 100% canonical-www). Removed `@astrojs/sitemap` integration → `sitemap-0.xml` returns 404 (consolidated; no longer duplicated). astro.config.mjs `site:` switched apex → www. robots.txt Sitemap directive updated. /branches/ + /branches/ashkelon/ × 3 langs now in sitemap (6 variants, all 200). normalizeSlug() helper fixes pre-existing malformed-URL bug ("supersale" → "/supersale/"). verify-sitemap.mjs PASS 9/9 on production. Pre-existing brand-slug 404s logged for follow-up REC. | — |
| REC-SITE-012 | MEDIUM | WP-drift sweep beyond phone+email — pre-2024 dates, "online checkout" verbiage in legal pages, third-party shortcodes. May benefit from Israeli consumer-law attorney engagement, not a Claude SPEC. | Daniel decides scope. |
| REC-SITE-013 | (closed) | M3_LIGHTHOUSE_NIGHTLY_CRON — closed 2026-05-10. Two workflows live on develop: lighthouse-daily.yml (03:00 IDT, 30 URLs, ~6-15 min) + lighthouse-weekly.yml (03:00 IDT Sunday, full sitemap, ~2 hr). Tooling under roles/site-overseer/tools/lighthouse/ (~222 MB node_modules, gitignored, CI-cached via actions/cache + package-lock.json hash). Reports: docs/guardian/lighthouse-reports/{daily,weekly}/{date}/. Regressions: append to docs/guardian/GUARDIAN_ALERTS.md below LIGHTHOUSE-CRON-APPEND-MARKER. First baseline (commit 83e5d9f, 2026-05-10): 24 OK + 6 SKIP_404 (M3-DATA-03), avg perf 87, avg a11y 95, ALL CLEAR. 5 commits in ERP repo, 0 storefront, 0 DB. Site Overseer SKILL bumped to v0.5. | — |
| REC-SITE-014 | (closed) | M3_REC014_ORPHAN_CLEANUP — closed 2026-05-09. Item A: 3 archived `/test-shortcodes/` rows (he/en/ru) hard-deleted from `storefront_pages` for prizma; backup JSON in SPEC folder; SC #1 met (post-DELETE COUNT=0); customer-facing impact zero (rows already 308'd or 404'd pre-fix; sitemap correctly omitted them). Item B: `_deprecated/` folder ALREADY GONE — Step 0b confirmed; no commit needed (likely closed by storefront commit `a4723b5` per REC-SITE-002 closure). Item C: 3 `poweredBy` keys removed from `src/i18n/{en,he,ru}.json:110`; pre-flight grep confirmed zero active references in `.astro/.ts/.tsx/.js`; storefront build PASS (5.98s); image-proxy guard clean. 2 commits total: `e84acd2` (ERP) + `2e2dd1b` (storefront, awaiting Daniel PR to main). | — |
| REC-SITE-015 | HIGH | **CLOSED 2026-05-08.** Phase A done; Phase B executed live for both subdomains via REST API; blog-post titles fuzzy-matched to specific Astro destinations (M3_WP_BLOG_POST_MAPPING). Final state: ru. and en. each have 1,610 live 301 redirects, 42-43 of them per-post specific (HIGH or LOW confidence) with `/blog/` index fallback for the ~3 unmatched. Phase C (WP decomm ~30 days post-Google-reindex) deferred. | Phase C: 1 SPEC, 1-2 hours. |
| REC-SITE-016 | LOW | Astro slug `/multi/` is too short; substring-matched `/multifocal/` (ru) and `/multifocal-glasses/` (en) WP slugs but is risky for future SEO collisions. Consider rename to `/multifocal-glasses/` with self-redirect — but only if SEO benefit is measured to outweigh the rename cost. | Investigation SPEC. Source: M3_WP_SUBDOMAINS_REDIRECT/FINDINGS.md M3-SEO-03. |
| REC-SITE-020 | (closed) | M3_QUICK_REGISTER_MARKETING_PRETICK_REMOVAL — closed 2026-05-13. Action (a) of pixel/consent triage executed. Single-line edit in `opticup-storefront` `src/pages/quick-register/index.astro:164` — `checked` attribute removed from `id="marketing"` checkbox. Storefront commit `ac6eef6`, pushed to `develop`, PR pending Daniel merge to `main` (compare URL: https://github.com/OpticaLis/opticup-storefront/compare/main...develop). `npm run build` PASS (4.37s); image-proxy guard PASS (9 files scanned, 0 violations). TERMS checkbox unchanged. Live verify (Criterion 11) deferred to post-merge Vercel deploy. Legal compliance: pre-ticked marketing consent invalid under Israeli Privacy Act 2024 amendment + Communications Act §30א. | — | 
| REC-SITE-021 | MEDIUM (DEFERRED) | **Bundled improvements for /quick-register/ pixel flow** — deferred by Daniel 2026-05-13 pending further conversation after REC-SITE-020 ships. Two sub-items: (B) expand marketing checkbox label text to also cover "קוקיז שיווקיים" so SuperSale form submitters auto-grant marketing-cookie consent (legal: not pre-ticked, has policy link); (C) wire `fbq('track','Lead')` to fire on successful form submit in `/quick-register/` if user consented — current 4 `pixel_events` rules in DB target `/successfulsupersale/` routes which don't exist (form shows inline success). Without (C), Pixel never receives Lead events from SuperSale signups. | 1 SPEC, ~1-2 hrs execute. |
| REC-SITE-019 | (closed) | M3_TIER1_CATEGORY_SLUG_FIX — closed 2026-05-10 via **Option B (replace, not build)**. Daniel verified live: actual routes are `/category/{slug}` (singular, no plural, no trailing slash) — all 6 (× 3 langs) return 200. Trivial 4-line edit in `roles/site-overseer/tools/lighthouse/config/tier1-pages.json` (2 paths + 2 reasons). Local re-baseline confirms 30 OK / 0 SKIP, avg perf 86, avg a11y 95. M3-DATA-03 (parent finding) effectively resolved. The 3rd option (build dedicated `/categories/{plural}/` routes) was rejected — `/category/{singular}` is the canonical storefront convention; building `/categories/{plural}/` would be redundant. | — |
| REC-SITE-017 | (closed) | M3_SITEMAP_BRAND_404_CLEANUP — closed 2026-05-09. Two-commit fix on storefront `develop` (`20eece1` + `4d0413f`), merged via Daniel PR + deployed. Sitemap brand block now filters by `brand_page_enabled = true AND product_count > 0` (mirrors ERP Studio surfaces + public `lib/brands.ts`). Production: 45 unique brand slugs in sitemap (down from 155), 15/15 random-sample probe = 200, total `<loc>` 254 in band [240,270]. Bonus: general-sample probe (check #8) returned 30/30 200 — M3_SITEMAP_CONSOLIDATION leftover "pre-existing data-quality issue" was entirely brand-block-driven and is now fully closed. New strict `brand404Probe()` regression check added to `verify-sitemap.mjs` (10 checks total, all PASS). 2 findings logged: M3-DEBT-12 (tenant-fallback-map.json drift) + M3-OBS-01 (verify check #8 stale warn) — both TECH_DEBT. | — |
| REC-SITE-018 | (closed) | 2026-05-09 — Daniel-authorized Level 2 SQL UPDATE executed: `UPDATE storefront_config SET custom_domain='www.prizma-optic.co.il' WHERE tenant_id=prizma`. Pre-flight value: `prizma-optic.co.il`. Post-update verified live: homepage `200`, `/brands/` `200`, `/branches/ashkelon/` `200`. OG/canonical/Twitter/hreflang/og:image all serve `https://www.prizma-optic.co.il/...` on every probed page. No code change required — `Astro.site` (canonical-www) was already authoritative for sitemap; this UPDATE brings every other `getBaseUrl` consumer (OG, JSON-LD, etc.) into the same truth. Closed-no-code. | — |

---

## Recent decisions (Daniel-authorized — full log in DECISIONS_LOG.md)

| Date | Decision | Action taken | Result |
|---|---|---|---|
| 2026-05-06 | "תתקן למספר 053-3645404 דחוף ותעשה דיפלוי" | UPDATE tenants SET business_phone='053-3645404' WHERE slug='prizma'. | Storefront verified correct on top-bar phone CTA. |
| 2026-05-06 | Drafted SPEC `M4_HARDCODED_DEMO_PHONE_CLEANUP` | Replaced decorative comment + corrected migration + LEARNINGS L-PROJECT-001. | Closed 2026-05-07 by opticup-executor. |
| 2026-05-06 | Drafted SPEC `M3_SITE_COMPREHENSIVE_REVIEW` for Mode A discovery | Site Overseer Foreman authored full audit SPEC. | This SPEC executed 2026-05-07 by opticup-executor — see audit deliverables. |
| 2026-05-08 | "Include all 3,221 with bulk mapping" — scope expansion in M3_WP_SUBDOMAINS_REDIRECT after URL count drift from 1,675 → 3,223 | Phase A executor crawled all 3,223 URLs across 9 sitemap types per subdomain; bulk-mapped extra 1,548 taxonomy/archive URLs via existing rules. | CSVs delivered (1,609 ru + 1,610 en) ready for Daniel's Phase B cPanel import. LEARNINGS L-SITE-001 added (subdomain enumeration rule). |
| 2026-05-08 | M3_WP_BLOG_POST_MAPPING — title-match per-post redirects + Site Overseer skill knowledge map | Executor: 42 ru + 43 en blog posts matched to Astro slugs (HIGH 71 / LOW 10 / NONE 4). Live mutations via Redirection plugin REST API: ru. surgical replace, en. full import (was 0 redirects). 5/5 ru + 5/5 en spot-check passes. SITE_OVERSEER_SKILL.md v0.2 created. | REC-SITE-015 fully closed; Phase C remains deferred. |
| 2026-05-08 | M3_CMS_BLOCKS_RESTORE_AND_GUARDRAIL — production hot-fix: 16 broken CMS pages + permanent CHECK constraints + L-PROJECT-002 + Site Overseer skill v0.3 | Executor: restored 16 storefront_pages rows via two-pass (15) + three-pass (1) unwrap of double-encoded blocks/previous_blocks. Installed CHECK constraints on both columns. Verified 16/16 live destinations now non-empty (40-65KB body). | REC-SITE-003 closed; bug class can no longer recur (DB-layer guardrail). |
| 2026-05-08 | M3_PHONE_434_LEGACY_CLEANUP — closure-only SPEC; storefront cleanup was already done by `a4723b5` (2026-05-07, Daniel) | Executor: Step 0 surfaced that all 3 target files were already deleted; AskUserQuestion to Daniel; chose ERP-retro-only path. ERP commit documents REC-SITE-002 closure. Findings logged: SPEC was authored on stale state (Foreman pre-flight gap). | REC-SITE-002 closed; defunct phone fully eliminated from customer-facing surface (CMS-row half + file-level half). |
| 2026-05-08 | M3_TENANT_NAME_FALLBACK_SAAS — SaaS-clean replacement of 28 `?? 'Optic Up'` fallbacks across storefront pages (audit anticipated 13) | Executor: Daniel approved scope expansion to all 28 (continued M3-SPEC-01 recurrence — third in 4 days); built generator + resolver function + JSON map; 11/11 unit tests PASS; storefront commit `a8c2acd` merged to main. | REC-SITE-006 closed; future tenant onboarding requires zero code changes for the name fallback. |
| 2026-05-09 | M3_COOKIE_CONSENT_OPT_IN — Israeli 2024 Privacy Protection Act compliance; tenant-config-driven Opt-In banner on production | Executor: 5 trackers wrapped in `consentGate()` (loader-script injection moved INSIDE gate for full pre-consent network silence); pre-script in BaseLayout stamps `window.__consent` before tracker payloads evaluate; banner reads `tenants.ui_config.cookie_consent`; required Daniel-authorized Level 3 view extension to expose `ui_config` via `v_public_tenant`; live test surfaced footer-link bug (prizma's `footer_config.columns` overrides defaultColumns) → fix-up commit `2aebe5a` refactored to bottom-bar; 6 live tests via Chrome DevTools MCP all PASS. | REC-SITE-010 closed; future tenant compliance requires only DB seed. 6 findings (1 MEDIUM SPEC gap, 1 MEDIUM exec miss, 4 INFO/LOW). |
| 2026-05-09 | M3_IMAGE_PROXY_ENFORCEMENT — Iron Rule 25 enforcement; route all images through `/api/image/...` proxy | Executor: pre-flight inventory found `resolveStorageUrl()` already existed with passthrough bug for full Supabase URLs (3 DB rows leaked: tenants.logo_url + 2 brand logos, all in `tenant-logos` bucket); fixed via regex-rewrite at the helper + extended proxy to support `tenant-logos/` bucket; new build-time check `scripts/check-no-direct-supabase-image.mjs` chained into `astro build` catches future regressions (synthetic test PASS — exit 1). Live verification on production via curl + Node script (Chrome MCP unresponsive): 14 pages, 146 sample image probes, 0 non-OK, 0 supabase leaks. Surfaced unrelated `404.astro` bug (passed full tenant to getThemeCSS) — fixed in fix-up commit `af32ad9`. | REC-SITE-007 closed. Follow-up SPEC needed: `M3_BUCKET_PRIVACY_FLIP` (Finding M3-COMPLIANCE-04). |
| 2026-05-09 | M3_BRANCHES_INFRA_AND_ASHKELON — per-branch infrastructure + first branch (Ashkelon) on production with Schema.org Local SEO | Executor: 6 DB migrations (table + RLS + view + Ashkelon seed); 12 storefront files (lib + 3 components + 6 page files + Footer link + verify script); Schema.org OpticalStore JSON-LD with 10 OpeningHoursSpecification entries (Tuesday-no-afternoon + lunch breaks) + 4 gallery images + sameAs GMB. verify-branches.mjs PASS 7/7. Required fix-up commit `ae60b37` removing pre-existing vercel.json `/branches/ → /terms-branches/` legacy redirect. Site Overseer SKILL bumped to v0.4 with new table/view in knowledge map. | REC-SITE-009 closed. Future branch/tenant onboarding requires only DB row. |
| 2026-05-09 | M3_SITEMAP_CONSOLIDATION — single canonical sitemap, switch to www, add /branches/ routes | Executor: switched astro.config.mjs `site:` apex→www; removed @astrojs/sitemap (sitemap-0.xml duplicate now 404); modified sitemap-dynamic.xml.ts to (a) read baseUrl from Astro.site (canonical), (b) add /branches/ static + per-branch DB query, (c) defensive normalizeSlug() helper fixing pre-existing malformed-URL bug; updated robots.txt to canonical www; verify-sitemap.mjs as ongoing regression script. Production: 364 locs + 986 hreflang alts, 100% www, 6/6 branch URLs 200, sitemap-0=404, robots canonical. AskUserQuestion to Daniel re. pre-existing brand-slug 404s (chose continue + log). 5 findings logged including 2 follow-up RECs (017 brand-404 cleanup, 018 getBaseUrl canonical-www). | REC-SITE-011 closed. Pre-existing data-quality issues surfaced as REC-SITE-017 + 018. |
| 2026-05-09 | REC-SITE-018 — flip `storefront_config.custom_domain` apex→www for prizma to bring every `getBaseUrl` consumer (OG/canonical/Twitter/hreflang/og:image) into truth | Site Overseer: pre-flight confirmed current value `prizma-optic.co.il`; Daniel-authorized Level 2 UPDATE executed; live re-fetch of `/`, `/brands/`, `/branches/ashkelon/` returned 200 with all meta tags serving `https://www.prizma-optic.co.il/...`. Zero code changes, zero deploy. | REC-SITE-018 closed-no-code. |
| 2026-05-09 | M3_SITEMAP_BRAND_404_CLEANUP — close REC-SITE-017; align sitemap brand emission with route + ERP Studio (155→45 URLs) | Foreman authored SPEC with SQL-equivalent inline (per A1 from prior FOREMAN_REVIEW), executor shipped 2 commits to storefront `develop`, Daniel merged + deployed via PR; production verified 9/9 SCs. Bonus: M3_SITEMAP_CONSOLIDATION leftover data-quality cluster fully resolved as side effect (general-sample probe 30/30 200, no residual non-brand 404s). Self-improvement loop working: A1 from M3_STUDIO_TRANSLATIONS_BRAND_FILTER FOREMAN_REVIEW prevented mid-execution AskUserQuestion on this SPEC. | REC-SITE-017 closed. 2 LOW findings → TECH_DEBT. |
| 2026-05-09 | M3_REC014_ORPHAN_CLEANUP — close REC-SITE-014; three orphan-cleanup items (DB rows + folder + i18n keys) | Foreman authored SPEC using all 6 newly-applied template improvements (subset-relationships sub-section, build-side-effect declaration for tenant-fallback-map.json, browser-readiness skip-line, etc. — first SPEC to exercise the full updated SPEC_TEMPLATE). Executor: Step 0 SQL confirmed exactly 3 archived `/test-shortcodes/` rows; Step 0b confirmed `_deprecated/` already gone + zero active `poweredBy` references in code. Item A: backup JSON committed in ERP repo + DELETE executed (3 rows → 0). Item B: skipped (already gone). Item C: 3 i18n JSON edits + build verified + tenant-fallback-map drift restored per template guidance. 2 commits: `e84acd2` (ERP) + `2e2dd1b` (storefront `develop`). | REC-SITE-014 closed pending Daniel PR-to-main of storefront `2e2dd1b`. Zero customer-facing impact at every stage. |
| 2026-05-10 | M3_LIGHTHOUSE_NIGHTLY_CRON — close REC-SITE-013; stand up Lighthouse + axe-core nightly + weekly monitoring | Foreman authored 269-line SPEC with explicit Step 0 + Step 0b scaffolding, Tier 1 page list grounded in Daniel's directive, all 9 SPEC_TEMPLATE improvements pre-applied. Executor: 5 commits in ERP repo (40fdbbc scaffold + b7300fc 6 scripts + 071e771 2 workflows + 83e5d9f baseline + this retro). One mid-execution AskUserQuestion (npm install size 222 MB > §4 200 MB threshold → Daniel chose `actions/cache`). Two pre-commit hook false-positives (Rule 21 on shared `main`/`round`/`totalElapsed` between run-tier1 and run-full) fixed by extracting `_lib.mjs` + renaming entry-points (no `--no-verify` bypass). First local baseline: 24 OK + 6 SKIP_404, avg perf 87, avg a11y 95. SC #17 (CI workflow_dispatch) deferred to Daniel via UI (gh CLI not auth'd in executor session). | REC-SITE-013 closed. SKILL → v0.5. 5 findings (1 MEDIUM content gap + 4 LOW/INFO execution observations). |
| 2026-05-13 | Pixel 304574492100180 verification + /quick-register/ consent triage. Daniel approved action (a) only: remove pre-tick from marketing-consent checkbox. Deferred (b) text expansion + (c) Lead pixel wiring pending further conversation. Two "can we make consent mandatory/pre-ticked" questions asked & legally declined twice. | Site Overseer: verified Pixel correctly wired on prod + consent-gated; documented REC-SITE-020 (PENDING) + REC-SITE-021 (DEFERRED) in this HANDOFF; logged DECISIONS_LOG entry. SPEC to be drafted next: `M3_QUICK_REGISTER_MARKETING_PRETICK_REMOVAL`. | REC-SITE-020 PENDING execution. Pixel itself: ✅ green. |
| 2026-05-10 | M3_TIER1_CATEGORY_SLUG_FIX — close REC-SITE-019 via Option B (replace, not build) | Daniel verified live the actual routes are `/category/{slug}` singular, not `/categories/{slug}/` plural. Foreman authored 4-line config-fix SPEC. Executor: Step 0 confirmed file unchanged + REC-019 still open + all 6 URLs return 200; 4-line edit in `tier1-pages.json` (2 paths + 2 reasons); ran local re-baseline; cleaned 6 stale `categories-{plural}` SKIP_404 JSONs from the date dir; regenerated SUMMARY.md (30 OK / 0 SKIP / avg perf 86 / avg a11y 95) + GUARDIAN_ALERTS section. Single commit. The new Step 1.5p URL existence verification rule (applied 2026-05-10 commit `0b00c9c`) self-validated: this SPEC's exhaustive §2 probe table is exactly the discipline that was missing in the parent M3_LIGHTHOUSE_NIGHTLY_CRON SPEC. | REC-SITE-019 closed. M3-DATA-03 effectively resolved. 0 findings (clean run). |

---

## Pending issues / future work — what's left after Mode A

### Tooling bootstrap (Mode B prerequisites)

- ~~**Lighthouse / lhci CLI**~~ — DONE 2026-05-10 (REC-SITE-013 / M3_LIGHTHOUSE_NIGHTLY_CRON). Lighthouse v12 installed under `roles/site-overseer/tools/lighthouse/`. Daily + weekly cron workflows on develop. See SITE_OVERSEER_SKILL.md §5d.
- ~~**axe-core CLI / pa11y**~~ — DONE 2026-05-10 (same SPEC). `@axe-core/cli` installed; the run-* scripts use Lighthouse's accessibility category which embeds axe-core, so violations are counted from LH's audit data. Standalone `axe` CLI is also available if a deeper a11y sweep is needed in future.
- **Chrome DevTools MCP / Playwright** — already available via MCP. Use for rendered-DOM checks in next Mode B run.
- **Visual regression DOM-hash baseline** — `scripts/visual-regression.mjs` (per docs/AUTONOMOUS_MODE.md). Snapshot Tier-1 anchor pages once DNS migrations are stable.

### Mode B operating procedure (proposal — Daniel approves)

For a targeted single-category audit:
1. Read SITE_MAP.md → §1 (routes) or §2 (values) to find scope.
2. Read the relevant section of SITE_AUDIT_REPORT.md to see prior findings.
3. Run targeted queries / fetches / DOM probes — single category, single concern.
4. Update SITE_MAP.md if drift detected.
5. If a finding cluster is large (>5 findings), promote to a full new audit SPEC. If small, append to a tracking issue or directly to a fix SPEC.

### Multi-tenant / SaaS readiness gates this audit revealed

Several findings reveal that the storefront codebase has not crossed the SaaS-readiness threshold for these surfaces:

- Lead-form `from:` is hardcoded "Optic Up Leads" — won't work for tenant 2.
- 13 files contain `'Optic Up'` fallback — same.
- Hardcoded address in `Footer.astro:104` defaultContact — should rely solely on storefront_config.
- Hardcoded font families in homepage `<link>` — Rubik+Assistant+Inter — even if a tenant prefers a different font.

This is a candidate for a Module-3-level "SaaS readiness Phase X" SPEC sweep — Daniel decides if/when.

### Live-vs-build-time field map (carried forward from 2026-05-06 HANDOFF)

- Confirmed empirically: `tenants.business_phone` is **live-DB-read** (DB UPDATE → user saw change without redeploy).
- Other tenant fields' live-vs-build-time status NOT yet mapped exhaustively. Mode B targeted task: enumerate which `tenants.*` and `storefront_config.*` fields require redeploy.

---

*End of SITE_OVERSEER_HANDOFF.md (v2 — Mode B).*
