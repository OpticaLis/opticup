# Activation Prompt — SITE_OVERSEER_KNOWLEDGE_BUILD_FUNNEL

> Paste the block below into a fresh Claude Code session.

---

טען את הסקיל `opticup-executor` ובצע את ה-SPEC הבא:

`roles/site-overseer/knowledge-build/SPEC.md`

הקשר קצר:

- **read-only בלבד.** אסור לכתוב, לעדכן או לתקן כלום. שום deploy, שום UPDATE, שום DELETE.
- המשימה: למפות 10 שכבות של ארכיטקטורת ה-CRM + המשפך + המדידה. תוצר: `KNOWLEDGE_MAP.md`.
- כל טענה חייבת ציטוט קוד או טבלה. ניחושים אסורים — אם לא בטוח, רשום כשאלה לדניאל.
- קומיט יחיד עם המפה.

בצע מקצה לקצה. עצור על כל גירוי "לתקן" משהו. דווח hash + סיכום של 5 פערי המדידה הכי חשובים שמצאת.